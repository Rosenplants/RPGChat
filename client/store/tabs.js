// Reducer
const testState = [{ name: 'CharInfo' }, { name: 'Notes' }, { name: 'Scenes' }];
export default (state = testState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
